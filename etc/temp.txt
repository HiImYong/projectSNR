<form class="" id="" method="POST" action="{%url 'visitor:newReview' racketItems.id%}">
                    <div><input id="inputNumForm" value="1"></div>
                    {%csrf_token%}
                    {%bootstrap_form racketReviewForm%} <!--render 함수에서 question_form 받아서 규격대로 생성-->
                    {%bootstrap_button button_type="submit" content="질문작성"%}
                    {%bootstrap_button button_type="reset" content="다시쓰기"%}
                </form>


def newReview(request, parameter):


    if request.method == "POST":
        getForm = ReviewForm(request.POST)

        if getForm.is_valid():
            savedReview = getForm.save(commit=False)
            savedReview.visitorAccount = request.user
            savedReview.visitorRacket_id = parameter
            savedReview.visitorScore = 5

            savedReview.save()
            messages.success(request, "리뷰가 등록되었습니다")
            return redirect('{}#anchor{}'.format(resolve_url('racket:racketDetail', parameter=parameter), 1))

    else:
        getForm = ReviewForm()
